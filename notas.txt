# RifaPay - Notas de Sesi�n (3 Jun 2026)

## Cambios implementados

### 1. P�ginas de Login, Register y Verify
- `src/app/login/page.tsx` - Formulario login con email + pass, redirige a `/verify` si email no verificado
- `src/app/register/page.tsx` - Registro en 2 pasos (datos del usuario -> c�digo de verificaci�n)
- `src/app/verify/page.tsx` - Ingreso manual de email + c�digo para verificar
- Links corregidos en homepage y detalle de rifa: apuntan a `/login` y `/register` en vez de `/api/auth/*`

### 2. P�gina /cart/pending
- `src/app/(public)/cart/pending/page.tsx` - P�gina para cuando MP devuelve pago pendiente
- Referenciada en `back_urls` de mercadopago.ts, antes daba 404

### 3. Preparaci�n para PRD (pospuesto)
- `master` actualizado con todos los fixes de `develop` (fast-forward merge, push a origin)
- `dotenv` agregado a `devDependencies` en package.json
- Supabase PRD no se cre�: l�mite de 2 proyectos free en la organizaci�n
- Render PRD y GitHub Actions ping workflow pendientes

### 4. Build verificado
- `npm run build` exitoso sin errores

### 5. Commit y push
- Rama `develop`: commit `97d213c` - "feat: login, register, verify pages + cart/pending + fix homepage links"
- Auto-deploy en Render QA desde develop

---

## Pendientes de QA

| # | Pendiente | Prioridad |
|---|-----------|-----------|
| 1 | Password recovery (recuperar/restablecer contrase�a) | Alta |
| 2 | Rate limiting en endpoints p�blicos | Alta |
| 3 | Dashboard sub-secciones: Mis rifas, Ventas, Afiliados | Media |
| 4 | P�ginas success/failure con detalle de la compra | Media |
| 5 | API de Afiliados | Baja |

---

## Pendiente de PRD (para cuando decidas)

| # | Pendiente | Estado |
|---|-----------|--------|
| 1 | Crear proyecto Supabase `rifapay-prd` | Pausado (l�mite free) |
| 2 | `prisma db push` contra conexi�n directa (5432) | Pausado |
| 3 | Crear Web Service en Render + env vars | Pausado |
| 4 | Workflow ping-render.yml en GitHub Actions | Pausado |

---

## Stack actual
- Next.js 16.2.7 (Turbopack), Node v24.15.0, React 19.2.4
- Prisma v7.8.0 + @prisma/adapter-pg + pg Pool
- PostgreSQL en Supabase (Transaction Pooler: aws-1-sa-east-1.pooler.supabase.com:6543)
- QA: Render (develop), URL: https://rifapay-qa.onrender.com

---

# RifaPay - Notas de Sesi�n (2 Jun 2026)

## Stack
- Next.js 16.2.7 (Turbopack), Node v24.15.0, React 19.2.4
- Prisma v7.8.0 + @prisma/adapter-pg + pg Pool (sin SSL para pooler)
- PostgreSQL en Supabase (Transaction Pooler: aws-1-sa-east-1.pooler.supabase.com:6543)
- QA: Render (develop), PRD: Cloudflare Pages (main)

## Base de Datos
- Supabase project ref: oikokixdgdsprirgwcv
- Pooler user: postgres.aoikokixdgdsprirgwcv
- DATABASE_URL: postgresql://postgres.aoikokixdgdsprirgwcv:xJO8I3zVNVFugqFq@aws-1-sa-east-1.pooler.supabase.com:6543/postgres
- Tablas creadas v�a script SQL directo (prisma db push no funciona sobre pooler)
- prisma generate ejecutado localmente

## Auth
- JWT_SECRET: ifapay-jwt-secret-cambiame-en-produccion (idem en Render)
- Flujo: register -> verify (c�digo 6 chars) -> login -> /api/auth/me
- Proxy inyecta x-user-id y x-user-role en requests autenticados

## Proxy (middleware)
- Paths p�blicos: /api/auth/*, /api/webhooks/mercadopago, /api/payments/create, /api/cart/add
- GET /api/rifas p�blico, POST /api/rifas requiere auth
- TODO lo dem�s requiere auth

## Mercado Pago
- App nueva creada en MP Developers para RifaPay (separada de GetAccess)
- MP_ACCESS_TOKEN (APP_USR-...) configurado en Render
- MP_WEBHOOK_URL configurado en Render: https://rifapay-qa.onrender.com/api/webhooks/mercadopago
- Webhook configurado en MP Dashboard (evento: Pagos, modo productivo)

## QA Deploy
- URL: https://rifapay-qa.onrender.com
- Rama: develop
- Render auto-deploy desde develop
- Env vars en Render: DATABASE_URL, JWT_SECRET, MP_ACCESS_TOKEN, MP_WEBHOOK_URL, NEXT_PUBLIC_URL

## API Endpoints
- GET /api/rifas - lista p�blica
- POST /api/rifas - crear (auth, crea ACTIVE + n�meros 1..N)
- GET /api/rifas/[slug] - detalle con n�meros
- POST /api/cart/add - p�blico, crea transaction + reserva n�meros (updateMany)
- POST /api/payments/create - p�blico, crea preferencia MP
- POST /api/webhooks/mercadopago - p�blico, recibe notificaciones MP
- GET /api/auth/me - auth, datos del usuario
- POST /api/auth/login - p�blico
- POST /api/auth/registro - p�blico
- POST /api/auth/verificar - p�blico
- GET /api/dashboard - auth, stats del usuario
- GET /api/payments/[id] - auth, estado del pago

## Frontend
- / - Home con listado de rifas
- /r/[slug] - Detalle de rifa con grilla de n�meros
- /cart?rifa=<slug> - Carrito con selector de n�meros y MP checkout
- /cart/success - Confirmaci�n
- /cart/failure - Error
- /dashboard - Panel del creador

## Prisma Schema (7 models: User, Rifa, Number, Transaction, Affiliate, AffiliateEarning)
- Enums: Plan, RifaStatus, NumStatus, TransStatus, EarnStatus
- Slug �nico por rifa (title + timestamp)
- N�meros se crean al crear la rifa (1..totalNumbers, status=AVAILABLE)
- Al comprar: se hace UPDATE de AVAILABLE a RESERVED + transactionId

## Bugs Corregidos
1. Rifa se creaba como DRAFT -> cambiado a ACTIVE
2. Cart/add intentaba crear n�meros nuevos -> cambiado a updateMany
3. Cart/add y payments/create requer�an auth -> agregados a bypass del proxy
4. Transaction create sin rifaId en nested create -> separado en dos calls

---

# RifaPay - Notas de Sesión (4 Jun 2026) - Migración a cuentas nuevas

## Resumen de cuentas nuevas

| Cuenta | GitHub | Supabase | Render |
|--------|--------|----------|--------|
| **Gmail nuevo** | `soporterifapay` | RifaPay QA + PRD | RifaPay QA + PRD |
| **Tu cuenta actual** | SupportGetAccess | GetAccess | GetAccess |

## URLs finales

| Servicio | URL |
|----------|-----|
| QA | https://rifapay-qa.onrender.com |
| PRD | https://rifapay-prd.onrender.com |

## Configuración de Render

### Build Command (QA y PRD)
```
npm install && npx prisma generate && npm run build
```

### Environment Variables - QA

| Variable | Valor |
|----------|-------|
| `DATABASE_URL` | `postgresql://postgres.pmolilfazhlplhaokamx:%40Supabase1982@aws-1-sa-east-1.pooler.supabase.com:6543/postgres` |
| `JWT_SECRET` | `rifapay-jwt-secret-cambiame-en-produccion` |
| `MP_ACCESS_TOKEN` | `APP_USR-2752992721428383-060217-0ab5d245e49d0b845478b4c4c87447be-1921694` |
| `MP_WEBHOOK_URL` | `https://rifapay-qa.onrender.com/api/webhooks/mercadopago` |
| `NEXT_PUBLIC_URL` | `https://rifapay-qa.onrender.com` |

### Environment Variables - PRD

| Variable | Valor |
|----------|-------|
| `DATABASE_URL` | `postgresql://postgres.pajryxchkqqxowjudrfe:%40Supabase1982@aws-1-sa-east-1.pooler.supabase.com:6543/postgres` |
| `JWT_SECRET` | `f8a2e9c1b4d7e6f3a4jd74km1az5f4c3a2b1c0d9e8f7a6b5c4d3e2f1a0b9c8` |
| `MP_ACCESS_TOKEN` | `APP_USR-2752992721428383-060217-0ab5d245e49d0b845478b4c4c87447be-1921694` |
| `MP_WEBHOOK_URL` | `https://rifapay-prd.onrender.com/api/webhooks/mercadopago` |
| `NEXT_PUBLIC_URL` | `https://rifapay-prd.onrender.com` |

## Supabase PRD Schema
- Se usaron tipos ENUM de PostgreSQL en vez de TEXT
- Enums creados: Plan, RifaStatus, NumStatus, TransStatus, EarnStatus
- Schema ejecutado desde SQL Editor en dos queries (enums + tablas)

## Fixes aplicados al código

### TypeScript - implicit any
| Archivo | Fix |
|---------|-----|
| `src/app/api/cart/add/route.ts:26` | Tipo explícito en callback `.map()` |
| `src/app/api/dashboard/route.ts:28-34` | Tipo `RifaStats` para resultado de Prisma |
| `src/app/api/rifas/route.ts:34` | Tipo `RifaRow` para resultado de Prisma |
| `src/app/api/rifas/[slug]/route.ts:38` | Tipo explícito en callback `.filter()` |

### Git
- Remote actualizado a `github.com/soporterifapay/rifapay.git`
- Rama `master` mergeada con `develop` (fast-forward)
- Workflow `.github/workflows/ping-render-prd.yml` creado en master (ping cada 14 min)

## Estado actual
- QA y PRD deployados y respondiendo correctamente
- QA con datos de pruebas previas
- PRD con base de datos vacía
