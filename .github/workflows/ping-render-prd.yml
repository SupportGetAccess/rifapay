name: Ping Render PRD

on:
  schedule:
    - cron: "*/14 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render PRD
        run: curl -s -o /dev/null -w "%{http_code}" https://rifapay-prd.onrender.com | grep -q 200
